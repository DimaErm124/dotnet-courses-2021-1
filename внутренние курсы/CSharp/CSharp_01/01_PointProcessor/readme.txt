Опишите те случаи, которые не учёл аналитик.
Но которые будет проверять тестировщик, и о чём он вас обязательно спросит.

Ответ:
При парсинге не рассмотрены варианты с отрицательными координатами.
При форматировании не рассмотрены варианты с отрицательными координатами.

Что будет делать программа, если очередная строка в файле пустая?
   о) пустой файл?
	Ответ:
	Ничего не произойдет. Программа завершится.
   о) файл не существует?
	Ответ:
	Программа попросит еще раз вести адрес файла. И так до тех пор пока либо не будет введен корректный адрес,либо - пустая строка.
   о) в строке файла буква?
	Ответ:
	Строка не будет распарсена.
   о) в строке файла три координаты?
	Ответ:
	Строка не будет распарсена.
   о) в строке файла одна координата?
	Ответ:
	Строка не будет распарсена.
   о) в строке файла только разделитель?
	Ответ:
	Строка не будет распарсена